{
  "name": "heituz-imggen2-skill",
  "version": "1.7.1",
  "private": false,
  "description": "Provenance-safe ChatGPT subscription image production skill.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "heituz-imggen2": "scripts/install.mjs",
    "heituz-imggen2-install": "scripts/install.mjs"
  },
  "files": [
    "SKILL.md",
    "README.md",
    "LICENSE",
    "contracts/**",
    "references/**",
    "scripts/*.py",
    "scripts/*.mjs",
    "scripts/fixtures/**"
  ],
  "scripts": {
    "test": "python3 -m unittest discover -s scripts -p 'test_*.py' -v && python3 -m py_compile scripts/*.py && node scripts/test_installer_privacy.mjs && node scripts/test_unified_installer.mjs && node scripts/test_vision_qc_setup.mjs",
    "smoke:install": "node scripts/install.mjs --offline --target /tmp/HeiTuzimgGen2-install-smoke",
    "smoke:runtime": "python3 -m unittest discover -s scripts -p 'test_*.py' -v"
  },
  "engines": {
    "node": ">=18"
  }
}
